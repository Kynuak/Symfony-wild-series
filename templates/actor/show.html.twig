{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}

    <h1>{{actor.name}}</h1>

    <section>
        <img src="{{ vich_uploader_asset(actor) }}" alt="" width="400px" height="500px">
        <h2>Dans les séries :</h2>
        <ul>
            {% for program in actor.programs %}
                <li><a href="{{ path('program_show', {programSlug: program.slug}) }}">{{ program.title }}</a></li>
            {% endfor %}
        </ul>
    </section>
{% endblock %}
